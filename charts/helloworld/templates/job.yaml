apiVersion: v1
kind: Pod
metadata:
  name: myapp-pod
  labels:
    app: myapp
  annotations:
    "helm.sh/hook": post-install,post-upgrade
spec:
  containers:
  - name: myapp-container
    image: 'debian'
    command: ["/bin/sh"]
    args: ["-c", "exit 1"]
  restartPolicy: Never
